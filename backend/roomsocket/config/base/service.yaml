apiVersion: v1
kind: Service
metadata:
  name: room-socket
spec:
  type: LoadBalancer
  selector:
    app: room-socket
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
status:
  loadBalancer: {}
