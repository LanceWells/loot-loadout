apiVersion: v1
kind: Service
metadata:
  name: roomsocket-service
spec:
  type: LoadBalancer
  selector:
    app: roomsocket-deployment
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
status:
  loadBalancer: {}
