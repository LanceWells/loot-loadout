apiVersion: v1
kind: Service
metadata:
  labels:
    app: room-socket
  name: room-socket
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8888
  selector:
    app: room-socket
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: room-socket
spec:
  replicas: 1
  selector:
    matchLabels:
      app: room-socket
  template:
    metadata:
      labels:
        app: room-socket
    spec:
      containers:
      - image: ghcr.io/lancewells/loot-loadout/room-socket
        name: roomsocket
        ports:
        - containerPort: 8888
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
